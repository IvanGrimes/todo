import React from 'react';
import PropTypes from 'prop-types';

const SignOut = props => {
  return (
    <button>
      Sign out
    </button>
  );
};

SignOut.propTypes = {

};

export default SignOut;
